
---

# `src/components/README.md`

````markdown
# Components

## Purpose

The `components` directory contains reusable UI components shared across
multiple features.

These components must remain generic and should not contain feature-specific
business logic.

## Folder Structure

```text
components/
├── common/
├── forms/
├── layout/
└── ui/
```